name: db-init
services:
  db-initializer:
    build:
      context: java
      dockerfile: ./Dockerfile
      args:
        ARTIFACT_ID: db-initializer
    restart: on-failure
networks:
  default:
    name: "master-project-network"
    external: true
