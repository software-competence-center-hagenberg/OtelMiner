spring:
  application:
    name: db-initializer
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:postgresql://database:5432/postgres
    username: db-user
    password: super-secure-12345
    driverClassName: org.postgresql.Driver
db-service:
  url: http://db-service:4242
test-data:
  file-paths:
    dynatrace: ${FILE_PATH_DYNATRACE}
    jaeger: ${FILE_PATH_JAEGER}
    sample: ${FILE_PATH_TRAIN_TICKET_SAMPLED}